{
  "name": "node-laravel-style-config",
  "version": "1.0.0",
  "description": "Laravel style config() and env() methods utilizing .env",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 src --out-dir dist",
    "jest": "jest",
    "tdd": "jest --watch"
  },
  "author": "Tom Myers",
  "license": "ISC",
  "dependencies": {
    "dot-object": "^1.6.0",
    "dotenv": "^4.0.0",
    "fs-extra": "^4.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-core": "^6.25.0",
    "babel-jest": "^20.0.3",
    "babel-preset-es2015": "^6.24.1",
    "jest": "^20.0.4",
    "jest-cli": "^20.0.4"
  }
}
